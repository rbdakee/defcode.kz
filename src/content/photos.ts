import type { StaticImageData } from "next/image";

import studioPhoto from "@/assets/photos/studio.jpg";
import processPhoto from "@/assets/photos/process.jpg";

/* Полоса снимков на «О компании» сейчас выключена — она перегружала блок.
   Файлы никуда не делись и лежат рядом: office-1.jpg, office-2.jpg,
   office-3.jpg. Чтобы вернуть — раскомментируйте импорты и положите
   их в массив office ниже. */
// import office1 from "@/assets/photos/office-1.jpg";
// import office2 from "@/assets/photos/office-2.jpg";
// import office3 from "@/assets/photos/office-3.jpg";

/**
 * ФОТОГРАФИИ САЙТА.
 *
 * Лежат в src/assets/photos, а НЕ в public/, и это принципиально.
 * Файл из public отдаётся по неизменному адресу, поэтому после замены
 * снимка браузер ещё четыре часа показывает старый из своего кеша —
 * ровно на это мы и напоролись. При импорте Next подмешивает в имя файла
 * хеш содержимого: меняется снимок — меняется адрес, и старому кадру
 * взяться неоткуда. Заодно из файла считываются размеры, поэтому вёрстка
 * не дёргается, пока картинка грузится.
 *
 * Как заменить: положите свой файл под тем же именем в src/assets/photos
 * и пересоберите. Пропорции любые — кадр везде задаёт вёрстка.
 * Поставьте null — блок со снимком исчезнет сам.
 *
 * TODO: своё здесь только studio.jpg. Остальное — сток с Pexels (лицензия
 * разрешает коммерческое использование без указания автора). Людей в кадре
 * нет намеренно: чужие лица рядом с рассказом о нашей работе читались бы
 * как наша команда, а это неправда. Заменить на свои до запуска.
 */
export const photos = {
  /** Крупный снимок в блоке «Как устроена работа» на «О компании». */
  studio: studioPhoto as StaticImageData | null,

  /** Полоса под ним. Пустой массив — полосы нет. */
  office: [] as StaticImageData[],

  /** Перед лестницей шагов на странице «Процесс». */
  process: processPhoto as StaticImageData | null,
};
